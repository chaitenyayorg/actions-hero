name: Show secrets
run-name: Show secrets

on:
    push:
        branches:
            - main
    
jobs:
    display-secrets-context:
        runs-on: ubuntu-latest
        steps:
            - run: echo "${{ toJson(secrets) }} | jq"

